#include<stdio.h>
main()
{
	int n,reverse=0,num=0;
	printf("enter the number:");
	scanf("%d",&n);
	num=n;
	while(num!=0)
	{
		reverse*=10;
		reverse+=num%10;
		num/=10;		
	}
	if(reverse==n)
		printf("entered number is palindrome");
	else
		printf("entered number is not palindrome");			
		
}

#include<stdio.h>
main()
{
	int i,n,flag=0;
	printf("enter any number: ");
	scanf("%d",&n);
	for(i=2;i<n/2;i++)
	{
		if(n%i==0)
			flag=1;
			break;	
	}
	if(flag==1)
		printf("entered number is not prime!!");
	else
		printf("entered number is  prime!!");
}
